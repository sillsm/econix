language: cpp

compiler: gcc

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq cmake 

before_script:
  - mkdir build
  - cd build
  - cmake ..
  - pwd

script: "make && pwd && sh ../sources/testing/travis_build.sh"

notifications:
  email:
    - max.sills@gmail.com
  on_success: change
  on_failure: alwayss
